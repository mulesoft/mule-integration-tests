mule:
    openTelemetry:
        meter:
            exporter:
                enabled: true
                type: GRPC
                interval: 1
                endpoint: ${mule.openTelemetry.meter.exporter.endpoint}