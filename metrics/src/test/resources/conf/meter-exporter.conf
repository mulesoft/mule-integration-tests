mule:
    openTelemetry:
        meter:
            exporter:
                enabled: true
                type: LOGGING
                interval: 1
                endpoint: ${mule.openTelemetry.meter.exporter.endpoint}