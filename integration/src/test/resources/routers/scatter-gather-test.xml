<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:test="http://www.mulesoft.org/schema/mule/test"
      xmlns:test-components="http://www.mulesoft.org/schema/mule/test-components"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xsi:schemaLocation="
               http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
               http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
               http://www.mulesoft.org/schema/mule/test http://www.mulesoft.org/schema/mule/test/current/mule-test.xsd
               http://www.mulesoft.org/schema/mule/test-components http://www.mulesoft.org/schema/mule/test-components/current/mule-test-components.xsd">

    <object name="threadCaptor" class="org.mule.test.routing.ThreadCaptor" />
    <object name="delayedProcessor" class="org.mule.test.routing.ScatterGatherRouterTestCase$DelayedMessageProcessor" />

    <flow name="minimalConfig" maxConcurrency="1">
        <scatter-gather>
            <route>
<!--                <set-payload value="apple"/>-->
                <flow-ref name="appleFlow"/>
            </route>
            <route>
<!--                <set-payload value="banana"/>-->
                <flow-ref name="bananaFlow"/>
            </route>
            <route>
<!--                <set-payload value="orange"/>-->
                <flow-ref name="orangeFlow"/>
            </route>
        </scatter-gather>
<!--        <flow-ref name="testRoutesMap"/>-->
    </flow>

    <flow name="appleFlow">
        <flow-ref name="delayedProcessor"/>
<!--        <set-payload value="apple"/>-->
    </flow>

    <flow name="bananaFlow">
        <flow-ref name="delayedProcessor"/>
<!--        <set-payload value="banana"/>-->
    </flow>

    <flow name="orangeFlow">
        <flow-ref name="delayedProcessor"/>
<!--        <set-payload value="orange"/>-->
    </flow>

    <sub-flow name="testRoutesMap">
        <test:assert expression="#[sizeOf(payload) == 3]"/>
        <test:assert expression="#[payload['0'].payload == 'apple']"/>
        <test:assert expression="#[payload['1'].payload == 'banana']"/>
        <test:assert expression="#[payload['2'].payload == 'orange']"/>
    </sub-flow>

</mule>
